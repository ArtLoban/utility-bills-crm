export type TCreateContractFormState = {
  providerId: string;
  validFrom: string;
  notes: string;
};

export type TChangeProviderFormState = {
  providerId: string;
  changeDate: string;
  notes: string;
};

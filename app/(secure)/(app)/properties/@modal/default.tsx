// Renders into the @modal slot when no intercepting route is active.
// Without this file, Next.js throws on hard navigation when no modal matches.
export default function ModalDefault() {
  return null;
}
